import 'package:flutter/material.dart';

class ResultySeachPage extends StatelessWidget {

  const ResultySeachPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}